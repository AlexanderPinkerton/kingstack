---
"@kingstack/create-kingstack": minor
---

Add existing-project port status, listing, registration, reassignment, and release commands. Reassignment moves legacy configurations to a safe contiguous block while preserving non-port local values and regenerating derived environment files.
