{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "yarn turbo build --filter=@kingstack/backend"
  },
  "deploy": {
    "startCommand": "yarn turbo start:prod --filter=@kingstack/backend"
  }
}